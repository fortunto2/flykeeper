<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Flykeeper</string>
	<!-- GENERATE_INFOPLIST_FILE is NO, so nothing fills these in for us. Without
	     CFBundleIdentifier the bundle installs and then fails to launch with "Missing
	     bundle ID", which reads like a signing problem and is not one. -->
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- No encryption beyond what the OS provides: declared here so every upload skips the
	     export-compliance question instead of waiting on it. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<!-- No NSAppTransportSecurity, no network usage keys, no entitlements file: this app
	     makes no outbound connection at all. Offline is the product, so the absence is
	     deliberate and pinned by a test rather than left to habit. -->
</dict>
</plist>
